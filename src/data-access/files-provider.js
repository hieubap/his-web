// import constants from '../assets/strings';
// import clientUtils from '../utils/client-utils';
// import crosstab from "crosstab";

export default {
    uploadFile(image) {
        // return new Promise((resolve, reject) => {
        //     clientUtils.uploadFile(constants.api.file.file, image).then(s => {
        //         resolve(s);
        //     }).catch(e => {
        //         if (e && e.status === 401) {
        //             localStorage.clear();
        //             crosstab.broadcast("message", {
        //                 event: "mediplus_logout",
        //             });
        //         }
        //         reject(e);
        //     })
        // });
    }
}   