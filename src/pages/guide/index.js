import React from "react";
import { Main } from "./styled";

const Guide = (props) => {
    return (
        <Main>
            Hướng dẫn sử dụng dịch vụ
        </Main>
    );
};

export default Guide;
