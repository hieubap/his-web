import styled from "styled-components";

export const Main = styled.div`
  .ant-input-group {
    display: flex;
    .ant-input {
      border: 2px solid rgba(9, 30, 66, 0.07);
      border-radius: 6px;
      margin-right: 7px;
      &:last-child {
        margin-left: 0;
      }
    }
  }
`;
